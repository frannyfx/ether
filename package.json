{
  "name": "ether",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "tsc && rm -rf dist/public && cp -Rf src/public dist/public && node dist/index.js",
    "pi-start": "tsc && rm -rf dist/public && cp -Rf src/public dist/public && sudo node dist/index.js",
    "clean": "rm -rf dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.0",
    "source-map-support": "^0.5.19",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "fastify": "^3.7.0",
    "fastify-static": "^3.3.0",
    "pigpio": "^3.2.4"
  }
}
